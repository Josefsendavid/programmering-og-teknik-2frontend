import React, { useState, useEffect } from "react";

const mainURL = "http://localhost:8080/startkodeca3"
const userInfo = "/api/info/user"
const hotels = "/api/hotel/all"
const bookings = "/api/hotel/bookings"

export{
    mainURL,
    userInfo,
    hotels,
    bookings,
}